server:
  port: ${PORT:8881}

spring:
  application:
    name: reservation-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: development

management:
  security:
    enabled: false
