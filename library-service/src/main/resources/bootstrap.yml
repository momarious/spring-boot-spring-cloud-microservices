server:
  port: ${PORT:8880}

spring:
  application:
    name: library-service
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: development

management:
  security:
    enabled: false
