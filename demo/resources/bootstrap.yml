server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native, development, production
  cloud:
    config:
      server:
        # git:
        #   uri: https://github.com/momarious/spring-server-config.git
        native:
          search-locations:
            classpath: /config  
    # compatibility-verifier:
    #   enabled: false
